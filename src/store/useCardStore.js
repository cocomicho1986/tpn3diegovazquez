//import { create } from 'zustand';

//const useCardStore = create((set) => ({
  //cardStates: { 1: false, 2: false, 3: false },
  //toggleCardView: (id) =>
    //set((state) => ({
    //  cardStates: { ...state.cardStates, [id]: !state.cardStates[id] },
  //  })),
//}));

//export default useCardStore;